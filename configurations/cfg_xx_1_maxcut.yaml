DATA:
  PARSE_METHOD: dat-s
  MATRIX_TYPE: dense (simple)
SOLVER:
  METHOD: R=inv(X)-kron-inv(X)
  HYPER_PARAMETERS:
    ITERATION_COUNT: 10000
    EPOCH_COUNT: 1
    RESTART_FACTOR: 1
    H: 0.01
  PREPROCESSING:
    MAX_CUT:
      IS_MAX_CUT_INSTANCE: True
